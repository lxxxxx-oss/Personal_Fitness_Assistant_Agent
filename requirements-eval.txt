-r requirements.txt

# Optional semantic RAG evaluation (not needed to run the application).
ragas>=0.4.0,<1.0.0
openai>=1.0.0,<3.0.0
# RAGAS 0.4.3 still imports chat_models.vertexai, removed in 0.4.2.
langchain-community>=0.4.1,<0.4.2
